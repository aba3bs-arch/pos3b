VITE_SUPABASE_URL=https://tu-proyecto.supabase.co
VITE_SUPABASE_ANON_KEY=tu_anon_key

# Opcional: fija la tienda en esta instalacion (MAIN, 3B5, etc.)
# VITE_SUCURSAL_FIJA=MAIN

# Web Push (avisos con app cerrada). Ver supabase/web_push_setup.md
# Genera claves con: npx web-push generate-vapid-keys
# VITE_VAPID_PUBLIC_KEY=BOxxxx_public_key_url_safe

# --- Netlify: agrega las mismas variables en el panel (Site configuration > Environment variables)
# Ver docs/NETLIFY.md
